// This file will later handle socket.io client setup
// For now, it only exports a placeholder to avoid crashes

export const socket = {
  connect: () => {
    console.warn("Socket not initialized yet");
  },
};
